/**
 * Sync module — stubbed out (leaderboard and cloud sync removed).
 */
const Sync = (() => {
    function onAnswer() {}
    function pushProgress() {}
    function pullProgress() {}
    function setupUnloadSync() {}
    return { onAnswer, pushProgress, pullProgress, setupUnloadSync };
})();
